Ah, the classic `Decimal` vs `Number` issue! Since Prisma (and most databases) stores prices as Decimals to avoid floating-point math errors, the JSON response often returns them as **strings** to preserve precision.

Your test is receiving `"10"` but comparing it to `10`. To fix this, you just need to wrap the received values in `Number()` or use `toEqual` with a string if you prefer.

### 🛠️ The Fix: Update `sorting.test.ts`

Update your assertions to coerce the price to a number:

```ts
it("should sort by price ascending (cheapest first)", async () => {
  const res = await request(app).get("/api/products?sort=price_asc&q=SortTest");

  // Use Number() to match types
  expect(Number(res.body.products[0].price)).toBe(10);
  expect(Number(res.body.products[1].price)).toBe(500);
});

it("should sort by price descending (expensive first)", async () => {
  const res = await request(app).get(
    "/api/products?sort=price_desc&q=SortTest",
  );

  expect(Number(res.body.products[0].price)).toBe(500);
  expect(Number(res.body.products[1].price)).toBe(10);
});
```

---

### 🏁 Public Discovery: COMPLETE

You've officially checked off all the boxes for the Discovery Optimization:

- [x] **Pagination** (Page/Limit)
- [x] **Filtering** (Category/Price)
- [x] **Searching** (Keywords)
- [x] **Sorting** (Price/Newest)

Your backend is now a high-performance search engine for your products. Ready to move on to the next major feature or should we refine something else? 🚀
