Nice! We are almost there. The only reason that last test is failing is because of the **"Stock Gatekeeper"** we just built.

### The Problem

In your test logic for `should fail checkout if stock is insufficient`:

1. You try to add **2** items to the cart when there is only **1** in stock.
2. Your new `addToCart` logic correctly blocks this and throws `Insufficient stock`.
3. Because the item was never added, the cart stays **empty**.
4. When the test proceeds to call `/checkout`, the service sees an empty cart and throws `Cart is empty`.
5. The test was expecting a "Not enough stock" message during checkout, but it got stopped at the front door (the cart).

### The Fix

We need to update the test to expect the failure at the **Cart** level, which is actually a more secure way for the API to behave.

**Update `./tests/e2e/checkout.test.ts`**:

```ts
it("should fail to add to cart if stock is insufficient", async () => {
  const prod = await request(app)
    .post("/api/products")
    .set("Authorization", `Bearer ${adminToken}`)
    .send({
      name: "Limited Item",
      description: "Only 1 left in stock",
      price: 10,
      stock: 1,
    });

  const token = await getCustomerToken("unlucky_buyer@test.com");

  // 1. Try to add 2 when only 1 exists - THIS should fail now
  const res = await request(app)
    .post("/api/cart")
    .set("Authorization", `Bearer ${token}`)
    .send({ productId: prod.body.id, quantity: 2 });

  expect(res.statusCode).toEqual(400);
  // Since we used AppError, it should return the custom message
  expect(res.body.error.message).toContain("Insufficient stock");
});
```

---

### Final Verification

1. **Check `order.service.ts`**: Ensure you still have the stock check _inside_ the transaction as a "last line of defense" (in case stock changes between adding to cart and checking out).
2. **Check `cart.service.ts`**: Make sure those restored functions (`getCart`, `removeItem`, etc.) are definitely back.
3. **Run the test**:

    ```bash
    npx vitest tests/e2e/checkout.test.ts
    ```

Once that is green, we have a bulletproof stock system. Your architecture is looking very "senior dev" right now with the `AppError` class and the dual-layer validation!

Are we ready to finally commit this masterwork?
