We have a few specific issues here. The `TypeError` tells us that when the test calls `getCart`, it's actually getting an undefined response, likely because of how the controller was refactored or how the service is being imported.

Also, the stock test is failing because it's hitting the `addToCart` validation **before** it even gets to the checkout, which is actually a good sign—our new validation is working!

Here is the plan to fix these E2E tests:

---

### 1. Fix the `getCart` Import Error

The error `TypeError: __vite_ssr_import_0__.getCart is not a function` usually happens if there is a mismatch in how `CartService` is exported or imported in the controller.

**Check `./src/controllers/cart.controller.ts`**:
Ensure you are importing the service correctly. If you are using `import * as CartService`, ensure the service file has named exports.

```ts
// Ensure this is correct in src/controllers/cart.controller.ts
import * as CartService from "../services/cart.service.js";

export const getCart = async (
  req: Request,
  res: Response,
  next: NextFunction,
) => {
  try {
    const userId = req.user!.id;
    const cart = await CartService.getCart(userId);

    // FIX: If cart is null, return items as empty array so .length doesn't crash
    if (!cart) {
      return res.json({ items: [] });
    }

    res.json(cart);
  } catch (error) {
    next(error);
  }
};
```

---

### 2. Fix the "Insufficient Stock" Test Logic

In your E2E test, you are trying to add **2** items to the cart when only **1** is in stock.

- **Old Behavior:** It added to cart, then failed at checkout.
- **New Behavior:** It fails immediately at `POST /api/cart`.

Because it fails to add to the cart, the cart remains empty. When the test then calls `/checkout`, it throws "Cart is empty".

**Update `./tests/e2e/checkout.test.ts`** to expect the error at the **Cart** level, OR bypass the cart validation for the sake of the checkout test:

```ts
it("should fail checkout if stock is insufficient", async () => {
  const prod = await request(app)
    .post("/api/products")
    .set("Authorization", `Bearer ${adminToken}`)
    .send({
      name: "Limited Item",
      description: "Only 1 left in stock",
      price: 10,
      stock: 1,
    });

  const token = await getCustomerToken("unlucky_buyer@test.com");

  // This request should now fail because of our new Cart validation!
  const cartRes = await request(app)
    .post("/api/cart")
    .set("Authorization", `Bearer ${token}`)
    .send({ productId: prod.body.id, quantity: 2 });

  expect(cartRes.statusCode).toBe(400);
  expect(cartRes.body.error.message).toContain("Insufficient stock");
});
```

---

### 3. Final Service Sync: `./src/services/order.service.ts`

Ensure the `checkout` service is using `AppError` so that the `order.controller.ts` (which we simplified) can pass the status code correctly.

```ts
// src/services/order.service.ts
import { AppError } from "../utils/appError.js";

// ... inside checkout transaction
if (!cart || cart.items.length === 0) {
  throw new AppError("Cart is empty", 400);
}

if (item.product.stock < item.quantity) {
  throw new AppError(`Not enough stock for ${item.product.name}`, 400);
}
```

### 🚀 Try running the E2E again

```bash
npx vitest tests/e2e/checkout.test.ts
```

The `TypeError` should vanish because we're handling the `null` cart case, and the stock test will pass because we're asserting on the correct step of the process. How's the terminal looking now?
